{% extends 'base.html.twig' %}

{% block title %}Recette: {{ recipe.name }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <!-- Titre de la recette -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-3 text-primary fw-bold">Nom de la recette: {{ recipe.name }}</h1>
        <a href="{{ path('app_recipe_index') }}" class="btn btn-outline-primary px-4 py-2 rounded-pill shadow-sm">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
    </div>

    <!-- Carte principale des détails -->
    <div class="card border-0 shadow-lg rounded-5 mb-5">
        <div class="card-header bg-primary text-white text-center py-4 rounded-5">
            <h5 class="mb-0">Détails de la Recette</h5>
        </div>
        <div class="card-body p-5">
            <table class="table table-borderless table-hover">
                <tbody>
                    <tr>
                        <th class="text-muted">Nom</th>
                        <td class="fw-bold text-dark">{{ recipe.name }}</td>
                    </tr>
                    <tr>
                        <th class="text-muted">Description</th>
                        <td class="fw-light text-secondary">{{ recipe.description }}</td>
                    </tr>
                    <tr>
                        <th class="text-muted">Slug</th>
                        <td class="fw-light text-secondary">{{ recipe.slug }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Actions de modification -->
    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="{{ path('app_recipe_edit', {'id': recipe.id}) }}" class="btn btn-warning px-4 py-2 rounded-pill text-white shadow-sm">
            <i class="fas fa-edit"></i> Modifier cette recette
        </a>
    </div>

    <!-- Bloc élégant de suppression -->
    <div class="mt-5 text-center">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <h4 class="alert-heading"><i class="fas fa-exclamation-triangle"></i> Attention !</h4>
            <p class="mb-0">Voulez-vous vraiment supprimer cette recette ? Cette action est irréversible.</p>
            <hr>
            <p class="mb-0">Si vous êtes sûr, cliquez sur le bouton ci-dessous pour confirmer la suppression.</p>
        </div>

         <div class="mt-5 text-center">
        
        <div class="d-flex justify-content-center">
            <div class="bg-light shadow-lg p-4 rounded-4">
                {{ include('recipe/_delete_form.html.twig') }}
            </div>
    </div>
</div>
{% endblock %}
